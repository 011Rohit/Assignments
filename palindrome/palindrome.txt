#include<iostream>
#include <bits/stdc++.h>
using namespace std;
int main()
{
    string one,two;
    cout<<"enter  string ";
    cin>>one;
    two=one;
    reverse(two.begin(), two.end());
    //two = one.reverse();

    if(one ==  two)
        cout<<"palindrome "<<endl;
    else
        cout<<"not"<<endl;


}

